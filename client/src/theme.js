import { createTheme } from '@mui/material/styles'

export default createTheme({
  palette: {
    mode: 'dark',
    primary: { main: '#ef5350' },
    secondary: { main: '#ffa726' },
    background: {
      default: '#121212',
      paper: '#1e1e1e',
    },
  },
})
